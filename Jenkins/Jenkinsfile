pipeline {
    agent any
    stages {
       stage('---clean---') {
                steps {
                 dir("project") {
                    sh "mvn clean -Dhttp.protocols=TLSv1.2"
                }
            }
        }
        stage('--test--') {
            steps {
                dir("project") {
                    sh "mvn test"
                }
            }
        }
       stage('--package--') {
            steps {
                dir("project") {
                    sh "mvn package -Dhttps.protocols=TLSv1.2"
                }
            }
        }
    }
}
