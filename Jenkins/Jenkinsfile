pipeline {
    agent any
    stages {
        stage('--test--') {
            steps {
                dir("project") {
                    bat "mvn test"
                }
            }
        }
        stage('--package--') {
            steps {
                dir("project") {
                    bat "mvn package"
                }
            }
        }
    }
}
